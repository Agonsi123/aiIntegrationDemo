# aiIntegrationDemo
A JavaScript  function that accepts a user prompt from an input and sends it to an AI text generation API, with a system prompt that defines the AI's behavior. Moderation checks are also implemented. This is a backend project.
